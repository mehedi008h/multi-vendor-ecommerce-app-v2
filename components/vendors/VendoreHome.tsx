import React from "react";
import { EventSlider } from "../shop";

const VendoreHome = () => {
    return (
        <div>
            <EventSlider />
        </div>
    );
};

export default VendoreHome;
