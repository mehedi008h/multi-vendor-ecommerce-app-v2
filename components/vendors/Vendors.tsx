import React from "react";
import { Vendor } from ".";

const Vendors = () => {
    return (
        <div className="grid grid-cols-12 gap-5">
            <Vendor />
            <Vendor />
            <Vendor />
            <Vendor />
            <Vendor />
        </div>
    );
};

export default Vendors;
