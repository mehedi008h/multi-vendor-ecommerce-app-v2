import React from "react";
import { LinkBar, SearchBar, TopHead } from ".";

const Navbar = () => {
    return (
        <>
            <TopHead />
            <SearchBar />
            <LinkBar />
        </>
    );
};

export default Navbar;
